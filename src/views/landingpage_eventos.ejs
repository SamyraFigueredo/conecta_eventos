<%- include('partials/header') %>

  <style>
    /* (Seu CSS original permanece igual, aqui não alterei nada para manter o estilo) */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #f9f9f9;
      color: #2C3E50;
    }

    header {
      background-color: #2C3E50;
      color: white;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem 2rem;
    }

    .logo {
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .logo img {
      height: 30px;
    }

    nav a {
      color: white;
      margin: 0 15px;
      text-decoration: none;
      font-size: 1rem;
    }

    nav a:hover,
    nav a.active {
      text-decoration: underline;
    }

    .btn-sair {
      background-color: white;
      color: #2C3E50;
      padding: 8px 20px;
      border: none;
      border-radius: 8px;
      font-weight: bold;
      cursor: pointer;
    }

    main {
      padding: 2rem;
    }

    .highlight {
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      background-color: white;
      padding: 2rem;
      border-radius: 12px;
      margin-bottom: 2rem;
    }

    .highlight-text {
      max-width: 500px;
    }

    .highlight-text h2 {
      font-size: 1.8rem;
      margin-bottom: 1rem;
    }

    .highlight-text p {
      font-size: 1rem;
      margin-bottom: 1.5rem;
    }

    .highlight-img img {
      max-width: 300px;
      border-radius: 10px;
      border: 1px solid #ccc;
    }

    .highlight button {
      background-color: #2C3E50;
      color: white;
      padding: 12px 20px;
      border: none;
      border-radius: 8px;
      font-size: 1rem;
      cursor: pointer;
      margin-top: 1rem;
    }

    .event-card {
      display: flex;
      align-items: center;
      justify-content: space-between;
      background-color: #2C3E50;
      color: white;
      border-radius: 12px;
      padding: 1.5rem;
      margin-bottom: 2rem;
      flex-wrap: wrap;
    }

    .event-card:nth-child(even) {
      background-color: #e74c3c;
      flex-direction: row-reverse;
    }

    .event-info {
      flex: 1;
      padding: 0 1rem;
    }

    .event-title {
      font-size: 1.5rem;
      font-weight: bold;
      margin-bottom: 1rem;
    }

    .event-date {
      font-size: 1rem;
      background-color: transparent;
      border: 2px solid #f1c40f;
      color: #f1c40f;
      display: inline-block;
      padding: 5px 10px;
      border-radius: 8px;
      margin-bottom: 1rem;
    }

    .event-card:nth-child(even) .event-date {
      border-color: #fff;
      color: white;
    }

    .event-card button {
      background-color: white;
      color: #2C3E50;
      border: none;
      border-radius: 6px;
      padding: 10px 20px;
      font-size: 1rem;
      cursor: pointer;
      font-weight: bold;
    }

    .event-img img {
      max-width: 160px;
      border-radius: 8px;
    }

    .inscritos-list {
      margin-top: 10px;
      font-size: 0.9rem;
      background-color: rgba(255, 255, 255, 0.15);
      padding: 10px;
      border-radius: 6px;
    }

    footer {
      text-align: center;
      font-size: 0.9rem;
      color: #888;
      padding: 1.5rem 0 0.5rem;
      border-top: 1px solid #ccc;
      margin-top: 3rem;
    }

    @media (max-width: 768px) {
      .highlight {
        flex-direction: column;
        align-items: flex-start;
        gap: 1rem;
      }

      .event-card {
        flex-direction: column;
        align-items: flex-start;
        gap: 1rem;
      }

      .event-card:nth-child(even) {
        flex-direction: column;
      }

      .event-img img {
        max-width: 100%;
      }
    }
  </style>

  <main>
    <section class="highlight">
      <div class="highlight-text">
        <h2>Descubra Eventos Incríveis. <br> Participe. Conecte-se.</h2>
        <p>Encontre os melhores eventos, palestras e workshops em um só lugar. Cadastre-se agora e garanta sua vaga nas
          experiências que vão transformar sua jornada.</p>
        <button onclick="alert('Inscrição iniciada')">Inscrever</button>
      </div>
      <div class="highlight-img">
        <img src="/img/evento_principal.svg" alt="Evento principal" />
      </div>
    </section>

    <% if (eventos && eventos.length> 0) { %>
      <% eventos.forEach(function(evento, idx) { %>
        <section class="event-card">
          <div class="event-img">
            <img src="/img/evento_bussines.svg" alt="Imagem do evento" />
          </div>
          <div class="event-info">
            <div class="event-title">
              <%= evento.nome_evento %>
            </div>
            <div class="event-date">
              <%= new Date(evento.data_inicio).toLocaleDateString('pt-BR', { day:'2-digit', month:'long', year:'numeric'
                }) %>
            </div>
            <p>
              <%= evento.descricao %>
            </p>
            <p><strong>Local:</strong>
              <%= evento.localizacao %>
            </p>
            <button onclick="alert('Você se inscreveu no evento <%= evento.nome_evento %>!')">Inscrever-se</button>

            <!-- Lista de inscritos (exemplo estático, você pode adaptar para dados reais) -->
            <% if (evento.inscritos && evento.inscritos.length> 0) { %>
              <div class="inscritos-list">
                <strong>Inscritos:</strong>
                <ul>
                  <% evento.inscritos.forEach(function(usuario) { %>
                    <li>
                      <%= usuario.nome_usuario %> (<%= usuario.tipo_usuario %>)
                    </li>
                    <% }) %>
                </ul>
              </div>
              <% } %>
          </div>
        </section>
        <% }) %>
          <% } else { %>
            <p>Nenhum evento cadastrado no momento.</p>
            <% } %>
  </main>

  <footer>
    Copyright © 2025 | Conecta Eventos
  </footer>

  <%- include('partials/footer') %>