# .env.test

# Configurações do banco de dados (ambiente de teste)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=sua_senha
DB_NAME=conecta_eventos_test

# Porta do servidor no ambiente de teste
PORT=4000

# Ambiente de execução
NODE_ENV=test

# Chave secreta para geração de tokens JWT (ambiente de teste)
JWT_SECRET=sua_chave_secreta_teste
